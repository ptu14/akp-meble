import { StockLeftPipe } from './stock-left.pipe';

describe('StockLeftPipe', () => {
  it('create an instance', () => {
    const pipe = new StockLeftPipe();
    expect(pipe).toBeTruthy();
  });
});
